repos:
  - repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: v0.0.262  # Use the latest version of ruff-pre-commit or pin to a specific version
    hooks:
      - id: ruff
        args: ["tests/", "--fix"]
